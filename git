alias ga="git add"
alias gs="git status"
alias gc="git commit -m"
alias gs="git status"
alias gl="git log"
alias gb="git branch"

alias push="git push"
alias pull="git pull"

# Clean that shit up
alias gbc="git branch --merged | grep -v '^* master$' | grep -v '^  master$' | xargs git branch -d"
alias wow="git log --all --graph --decorate --oneline --simplify-by-decoration"
